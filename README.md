# -30-

存储

## Countdown Script

This repository includes a simple countdown script written in Python.

### Usage

```bash
python3 countdown.py [seconds]
```

If `[seconds]` is omitted, the script defaults to 10 seconds.
